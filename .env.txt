# --- Database Configuration ---
# Main System Database
DB_USER=postgres
DB_PASSWORD=change_this_password_for_production_please
DB_NAME=inercia_base

# --- Multi-Project Pre-provisioning (Recommendation) ---
# If using Docker, you can pre-create multiple databases in the same instance
# or use multiple postgres containers. This BaaS manages connections dynamically.
NUMBER_OF_PROJECTS=3

# --- Redis Configuration ---
REDIS_PASSWORD=inercia_redis_secure_pass_123

# --- Security Secrets ---
JWT_SECRET=production_jwt_secret_key_change_me_immediately_xyz_789
SESSION_SECRET=production_session_secret_change_me
JWT_EXPIRY=3600

# --- Domain & Network Configuration ---
DOMAIN_NAME=inercia.unibloom.shop
API_DOMAIN_NAME=api.unibloom.shop

# Full URLs for Application Logic
FRONTEND_URL=https://inercia.unibloom.shop
API_EXTERNAL_URL=https://api.unibloom.shop
CALLBACK_URL=https://inercia.unibloom.shop/api/auth/callback

# --- Application Config ---
PORT=3000
NODE_ENV=production

# --- Super Admin (Studio Access) ---
SUPER_ADMIN_EMAIL=admin@inercia.io
SUPER_ADMIN_PASSWORD=admin123

# --- System ---
GENERIC_TIMEZONE=America/Sao_Paulo
POOLER_DEFAULT_POOL_SIZE=20