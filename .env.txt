# Database Configuration
DB_USER=postgres
DB_PASSWORD=secure_production_password_change_me
DB_NAME=inercia_prod

# Secrets (Generate new random strings for production)
JWT_SECRET=super_secret_jwt_key_must_be_long_random_string_xyz_123
SESSION_SECRET=another_long_random_string_for_sessions

# API Configuration
PORT=3000
NODE_ENV=production

# URLs (Adjust these to your actual VPS IP or Domain)
# Example: http://192.168.1.100 or https://api.inercia.io
FRONTEND_URL=http://localhost:80
CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# OAuth (Optional - leave empty if not using yet)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
